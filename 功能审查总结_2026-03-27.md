# Chen404 功能审查总结

> 历史快照：本文保留 2026-03-27 的原始审查结论，不应再作为当前缺陷清单。当前扫描结果请查看 [`doc/architecture/前后端代码与文档扫描报告_2026-08-14.md`](./doc/architecture/前后端代码与文档扫描报告_2026-08-14.md)。

> 审查日期：2026-03-27
> 审查范围：前端 (Chen404Fro) + 后端 (Chen404Bac) 全功能

---

## 一、整体实现状态

| 模块 | 状态 | 备注 |
|------|------|------|
| 用户认证 | ✅ 完整 | 登录/注册/验证码/密码修改 |
| 权限系统 | ✅ 完整 | RBAC + 信任级别 + 内容可见性 |
| 文章管理 | ✅ 完整 | CRUD + 可见性 + 评论策略 |
| 分类管理 | ✅ 完整 | 后台管理 + 前台展示 |
| 标签管理 | ✅ 完整 | 自动创建 + 关联查询 |
| 评论系统 | ✅ 完整 | 文章评论 + 留言板 + 游客删除 |
| 文件上传 | ✅ 完整 | 图片/封面/头像 + SysFile 管理 |
| 首页聚合 | ✅ 完整 | 统计数据 + 热门文章 + 最新评论 |
| 个人中心 | ✅ 完整 | 资料管理 + 我的文章 |
| 后台管理 | ⚠️ 部分 | 分类管理完成，用户管理待完善 |
| 归档/标签页 | ⚠️ 部分 | 部分使用模拟数据 |

---

## 二、已发现的 Bug / 遗漏

### 🔴 高优先级（已修复）

| # | 问题 | 位置 | 修复方式 |
|---|------|------|----------|
| 1 | Header 编写按钮未做权限控制 | `Header.vue` | 添加 `canCreateArticle` 判断 |
| 2 | 文章卡片删除按钮无权限判断 | `ArticleCard.vue` | 根据 `canEdit`/`canDelete` 显示 |
| 3 | 游客评论自助删除缺失 | `CommentSection.vue` | 实现 localStorage 存储 deleteKey |

### 🟡 中优先级（待处理）

| # | 问题 | 位置 | 建议修复 |
|---|------|------|----------|
| 1 | `fillArticlePermissions` 重复查库 | `AccessServiceImpl` | 缓存 user 对象避免重复查询 |
| 2 | `getArticlePage` 的 `status` 参数被忽略 | `ArticleServiceImpl` | 公开列表移除该参数 |
| 3 | Logout 无服务端失效 | `AuthController` | Redis token 黑名单 |
| 4 | 后台管理用户列表缺失 | `Admin.vue` | 添加用户管理表格 |
| 5 | 信任级别管理前端入口缺失 | `Admin.vue` | 添加设置信任级别按钮 |

### 🟢 低优先级（待处理）

| # | 问题 | 位置 | 建议修复 |
|---|------|------|----------|
| 1 | `Article.password` 与 `visibility` 并存 | 数据库设计 | 明确关系或废弃 password |
| 2 | 返回实体未经 DTO 裁剪 | 多处 | 引入 ArticleVO/UserVO |
| 3 | 前端路由守卫基于 localStorage | `router/index.ts` | 增加 `/auth/info` 校验 |
| 4 | 归档页面部分模拟数据 | `Archive.vue` | 接入真实归档接口 |
| 5 | 标签页面部分模拟数据 | `Tag.vue` | 接入真实标签接口 |

---

## 三、权限系统审查详情

### 3.1 后端权限实现 ✅

| 组件 | 文件路径 | 状态 |
|------|----------|------|
| JWT 拦截器 | `interceptor/JwtInterceptor.java` | ✅ 公开接口识别 + 禁用用户拦截 |
| 管理员切面 | `aspect/RequireAdminAspect.java` | ✅ 角色校验 |
| 权限服务 | `service/AccessService.java` | ✅ 完整判定逻辑 |
| 文章权限 | `service/ArticleServiceImpl.java` | ✅ 创建/更新/删除权限控制 |
| 评论权限 | `service/CommentServiceImpl.java` | ✅ 评论策略 + 游客删除 |
| 文件权限 | `service/SysFileServiceImpl.java` | ✅ 所有权校验 |
| 用户信任级别 | `controller/AdminUserController.java` | ✅ 管理接口已提供 |

### 3.2 前端权限配合 ✅

| 组件 | 文件路径 | 状态 |
|------|----------|------|
| 权限工具 | `utils/permission.ts` | ✅ `isAdminUser`/`isFriendUser` |
| 路由守卫 | `router/index.ts` | ✅ `requiresAuth`/`requiresAdmin` |
| Header | `components/Header/Header.vue` | ✅ 角色展示 + 编写按钮控制 |
| Profile | `views/Profile/Profile.vue` | ✅ 信任级别展示 |
| 文章详情 | `views/Article/ArticleDetail.vue` | ✅ 编辑按钮控制 |
| 文章编辑 | `views/Article/ArticleEdit.vue` | ✅ 可见性/评论策略设置 |
| 评论组件 | `components/Comment/` | ✅ 游客删除支持 |

---

## 四、API 完整性检查

### 4.1 已实现的 API ✅

```
# 认证
POST   /auth/login
POST   /auth/register
POST   /auth/send-code
POST   /auth/refresh
GET    /auth/check-username
GET    /auth/check-email
GET    /auth/check-phone

# 用户
GET    /user/info
PUT    /user/profile
PUT    /user/password

# 文章
GET    /articles              # 列表（支持分类/标签筛选）
POST   /articles              # 创建（需受信任用户）
GET    /articles/mine         # 我的文章
GET    /articles/{id}         # 详情
PUT    /articles/{id}         # 更新（仅管理员）
DELETE /articles/{id}         # 删除（仅管理员）
POST   /articles/{id}/like    # 点赞（限流 60s）
GET    /articles/{id}/neighbors # 上一篇/下一篇
GET    /articles/hot          # 热门文章
GET    /articles/recommend    # 推荐文章

# 分类
GET    /categories            # 列表
GET    /categories/{id}       # 详情
POST   /categories            # 创建（仅管理员）
PUT    /categories/{id}       # 更新（仅管理员）
DELETE /categories/{id}       # 删除（仅管理员）

# 评论
GET    /comments              # 文章评论列表
GET    /comments/guestbook    # 留言板评论
POST   /comments              # 发表评论
DELETE /comments/{id}         # 删除评论
POST   /comments/{id}/like    # 点赞评论

# 文件上传
POST   /upload/image          # 上传图片
POST   /upload/images         # 批量上传
POST   /upload/cover          # 上传封面
POST   /upload/avatar         # 上传头像
DELETE /upload/file           # 删除文件

# 首页/站点
GET    /home                  # 首页聚合数据
GET    /site/stats            # 站点统计

# 管理员
PUT    /admin/users/{id}/trust-level  # 设置信任级别
```

### 4.2 待实现的 API ⏳

```
# 后台管理
GET    /admin/users           # 用户列表
GET    /admin/comments        # 评论管理列表
PUT    /admin/comments/{id}/review  # 审核评论

# 归档
GET    /archives              # 归档数据
```

---

## 五、数据库结构审查

### 5.1 核心表 ✅

| 表名 | 状态 | 说明 |
|------|------|------|
| `sys_user` | ✅ | 用户表，含 `trust_level` 字段 |
| `sys_role` | ✅ | 角色表 |
| `sys_user_role` | ✅ | 用户角色关联 |
| `article` | ✅ | 文章表，含 `visibility`/`comment_policy` |
| `category` | ✅ | 分类表 |
| `tag` | ✅ | 标签表 |
| `article_tag` | ✅ | 文章标签关联 |
| `comment` | ✅ | 评论表 |
| `comment_guest_token` | ✅ | 游客评论删除 Token |
| `sys_file` | ✅ | 文件管理表 |

### 5.2 SQL 脚本位置

```
bac_doc/sql/
├── 3-20/
│   ├── chen404.sql                  # 完整数据库
│   └── permission-phase1-upgrade.sql # 权限升级脚本
├── 3-26/
│   └── chen404.sql                  # 更新版数据库
└── 3-27/
    ├── comment-guest-token.sql      # 游客Token表
    ├── drop-friend-link.sql
    └── seed-permission-min.sql      # 最小权限数据
```

---

## 六、前端组件审查

### 6.1 页面组件 ✅

| 页面 | 路径 | 状态 |
|------|------|------|
| 首页 | `views/Home/Home.vue` | ✅ 完整 |
| 文章详情 | `views/Article/ArticleDetail.vue` | ✅ 完整 |
| 文章编辑 | `views/Article/ArticleEdit.vue` | ✅ 完整 |
| 分类 | `views/Category/Category.vue` | ✅ 完整 |
| 标签 | `views/Tag/Tag.vue` | ⚠️ 部分模拟数据 |
| 归档 | `views/Archive/Archive.vue` | ⚠️ 部分模拟数据 |
| 留言板 | `views/Guestbook/Guestbook.vue` | ✅ 完整 |
| 关于 | `views/About/About.vue` | ✅ 完整 |
| 登录/注册 | `views/Auth/` | ✅ 完整 |
| 个人中心 | `views/Profile/Profile.vue` | ✅ 完整 |
| 后台管理 | `views/Admin/AdminLayout.vue` | ⚠️ 仅分类管理 |

### 6.2 公共组件 ✅

| 组件 | 路径 | 状态 |
|------|------|------|
| Header | `components/Header/Header.vue` | ✅ 完整 |
| Footer | `components/Footer/Footer.vue` | ✅ 完整 |
| ArticleCard | `components/ArticleCard/ArticleCard.vue` | ✅ 完整 |
| CommentSection | `components/Comment/CommentSection.vue` | ✅ 完整 |
| CommentItem | `components/Comment/CommentItem.vue` | ✅ 完整 |
| Live2D | `components/Live2D/Live2D.vue` | ✅ 完整 |
| SakuraOverlay | `components/SakuraOverlay/SakuraOverlay.vue` | ✅ 完整 |

---

## 七、文档目录结构

审查后文档统一放置于后端项目：

```
Chen404Bac/
├── fro_doc/                    # 前端文档（原 Chen404Fro/doc）
│   ├── architecture.md         # 前端架构设计
│   └── bug.md                  # 前端 Bug 清单
│
├── bac_doc/                    # 后端文档（原 Chen404Bac/doc）
│   ├── architecture/
│   │   ├── architecture.md     # 后端架构设计
│   │   ├── permission-design.md    # 权限设计
│   │   └── permission-summary.md   # 权限总结
│   ├── bug/
│   │   └── bug.md              # 后端 Bug 清单
│   ├── plan/                   # 计划文件（历史）
│   └── sql/                    # 数据库脚本
│
└── 功能审查总结_2026-03-27.md   # 本文件
```

---

## 八、后续建议

### 8.1 立即处理

1. **后台管理用户列表**：实现 `/admin/users` 接口和前端表格
2. **信任级别管理前端入口**：在后台添加设置信任级别按钮

### 8.2 短期优化

1. **归档/标签页数据对接**：移除模拟数据
2. **性能优化**：`fillArticlePermissions` 减少重复查库

### 8.3 长期规划

1. **响应 DTO 裁剪**：引入 VO 层避免字段暴露
2. **Token 黑名单**：Redis 存储已注销 token
3. **文章密码保护**：明确与 `visibility` 的关系

---

## 九、审查结论

**整体评价**：项目功能完整度约 **85%**，核心功能（认证、权限、文章、评论）均已完整实现。

**亮点**：
- 权限设计完善，前后端配合良好
- 游客评论自助删除机制完整
- 文件上传使用 SysFile 统一管理
- 点赞限流已防护

**待完善**：
- 后台管理功能需补充用户管理
- 部分页面仍有模拟数据
- 归档/标签需完整对接
