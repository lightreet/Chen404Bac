# Sakura Radio 音乐功能需求与 UI 设计

> 当前同步时间：2026-08-14。本文维护“当前实现 + 设计约束 + 后续优化清单”。前端实现位于 `Chen404Fro/src/views/Music`、`src/stores/music-player.ts`、`src/components/Live2D/Live2DMusicPanel.vue`；后端实现位于 `MusicRadioController`、`MusicRadioServiceImpl`、`MusicPlayerStateServiceImpl` 和 `V2026052701__create_music_radio_tables.sql`。

## 0. 当前实现状态

已落地：

- `/music` 公开音乐馆，展示公开歌曲、公开歌单、默认电台、推荐语、歌词和唱片架。
- 顶部导航新增“音乐馆”，Live2D 工具栏新增音乐入口。
- `music-player` Pinia store 统一驱动音乐页和 Lyra 随身播放器。
- 具备 `music:create` capability 的知友和管理员可维护自己的歌曲；管理员额外维护全站歌曲、歌单排序和默认电台。
- 新增/编辑歌曲采用独立工作台页面：`/music/tracks/new`、`/music/tracks/:id/edit`。
- 支持音乐音频上传、封面上传、纯文本歌词、基础 LRC 歌词校验。
- 支持 AI 曲目信息补全：`POST /admin/music/tracks/ai/suggest`。
- 后端公开接口只返回 `published` 歌曲和公开歌单；创作者接口校验贡献者所有权，管理接口需要管理员权限。
- 歌曲删除会先移除歌单关系；音乐音频/封面已纳入 `file_reference`，并提供专用引用重建任务。
- 歌单删除已实现；删除默认歌单时由服务端维护默认状态约束。
- 登录用户的队列、当前歌曲、当前进度和播放模式保存在 Redis，默认滑动过期 7 天；游客现场保存在浏览器本地。

尚未完成：

- 播放统计、收藏/点赞、评论、播放历史和推荐算法。
- Media Session 系统媒体控制。
- `/music?playlist={id}` 和 `/music?track={id}` 深链播放当前尚未作为稳定入口承诺。

## 1. 设计背景

Chen404 是一个带有强烈个人气质的网站。当前首页已经形成了清晰的视觉语言：樱花粉、柔雾背景、玻璃胶囊导航、手写感标题、Lyra 女仆悬浮助手和轻盈的文章卡片。音乐功能应当延续这个世界观，成为“网站里可以被打开的一间小电台”，而不是传统后台里的文件管理功能。

本功能定名为 **Sakura Radio**。它由两部分组成：

- **公开音乐馆**：主功能页面 `/music`，用于展示当前公开音乐、歌单、歌词和站长推荐语。
- **Lyra 随身播放器**：女仆工具栏上的音乐按钮，点击后弹出小播放器并自动播放默认电台。

音乐内容不放入传统后台列表。参考旅行地图，音乐页面本身是公开内容页，普通用户可以浏览和播放；具备创作能力的用户在同一页面看到自己的编辑入口，管理员还能维护全站曲目与歌单。

### 1.1 现站点视觉依据

已确认线上站点 `https://www.chen404.cn/` 为 Chen404 Blog 前端入口。结合当前首页截图和现有前端代码，音乐页需要继承以下特征：

- 顶部导航是居中的玻璃胶囊，当前项用浅粉白底和粉色图标强调。
- 首页 Hero 是大图背景、粉白雾化遮罩、手写感大标题和柔波浪底部过渡。
- Lyra 位于页面左下区域，工具栏是小圆形按钮组。
- 页面主体内容居中，宽度克制，Discovery 区和文章卡片保留大量留白。
- 颜色不是高饱和粉色铺满，而是暖白、柔灰、浅粉和淡紫雾共同构成。

## 2. 产品目标

- 在网站主导航中新增“音乐馆”或“Sakura Radio”入口。
- 用户点击女仆音乐图标后弹出小播放器，并在用户点击触发后自动播放。
- `/music` 页面展示公开歌单、当前播放歌曲、歌词、推荐语和唱片架。
- 创作者在 `/music` 页面进入独立歌曲工作台维护自己的曲目；管理员在同页维护歌单和全站内容。
- 支持完整歌曲资料：音频、封面、歌名、歌手、专辑、年份、语言、标签、推荐语、歌词。
- 歌词支持纯文本和 LRC 时间轴双模式。
- 视觉上延续 Chen404 当前首页：樱花、暖白纸感、玻璃胶囊、淡粉雾面、轻柔动效。

## 3. 非目标

- 首版不做用户评论、点赞、收藏、播放排行榜。
- 当前不做投稿审核流；知友创作者可直接保存草稿或发布本人曲目，管理员保留治理能力。
- 首版不做第三方音乐平台同步。
- 首版不强制歌曲关联文章或旅行地图，只预留字段扩展。
- 首版不做复杂歌词编辑器，只做文本输入、LRC 校验和播放预览。

## 4. 用户与权限

### 4.1 普通访客

- 可访问 `/music`。
- 可查看已上架歌曲、公开歌单、封面、歌词、标签、推荐语。
- 可播放、暂停、切歌、拖动进度、调节音量、切换播放模式。
- 不可看到编辑入口。
- 不可访问保存、上传、删除、上下架接口。

### 4.2 知友创作者

- 需要启用账号、知友身份、`music:create` capability 和对应运行时开关。
- 可新增曲目，查看、编辑、变更状态和删除自己的曲目。
- 不可维护他人曲目、歌单、默认电台，也不可调用 AI 补全管理接口。

### 4.3 管理员

- 拥有普通访客所有能力。
- 可在 `/music` 页面直接新增、编辑、上下架、删除全站歌曲。
- 可上传音频和封面。
- 可维护纯文本歌词或 LRC 歌词。
- 可维护歌单、默认电台和歌单歌曲排序。
- 可查看草稿、下架歌曲和非公开歌单。

## 5. 信息架构

### 5.1 主路由

- `/music`：公开音乐馆。
- `/music/tracks/new`：新建歌曲工作台，需 `music:create`。
- `/music/tracks/:id/edit`：编辑本人歌曲或管理员编辑，后端校验所有权。
- `/music?playlist={id}`、`/music?track={id}`：仍是待稳定的深链能力。

不新增传统后台路由。歌曲新增/编辑跳转到独立工作台，管理员歌单维护使用 `/music` 页面内抽屉。

### 5.2 导航位置

建议在当前顶部导航中加入“音乐馆”：

- 位置建议：`旅行地图` 后，`受信申请` 前。
- 图标建议：Element Plus `Headset`、`Service` 或 `Microphone`。如果图标库没有合适图标，可优先使用 `Headset`。
- 文案建议：桌面端显示“音乐馆”，移动端显示“音乐馆”。

## 6. UI 设计图

以下线框图用于表达结构、层级和交互关系。真实视觉实现应贴合当前站点首页截图中的樱花 Hero、玻璃导航和柔雾页面风格。

### 6.1 桌面端音乐页

```text
+--------------------------------------------------------------------------------+
|  顶部玻璃导航                                                                  |
|  Logo   首页 分类 时光轴 旅行地图 音乐馆 受信申请 留言板 关于       编写 月亮 头像 |
+--------------------------------------------------------------------------------+

+--------------------------------------------------------------------------------+
|  Hero: Sakura Radio                                                             |
|  背景：柔粉樱花/唱片架/夜读桌面图，叠加淡雾和花瓣                              |
|                                                                                |
|                       Sakura Radio                                              |
|                    今晚，Lyra 为你调好一首歌。                                  |
|                                                                                |
|  Lyra 悬浮在左下，工具栏新增音乐按钮                                             |
+--------------------------------------------------------------------------------+

                     ↓

+--------------------------------------------------------------------------------+
|  主播放器区                                                                     |
|  +--------------------+  +---------------------------------------------------+  |
|  | 封面/唱片旋转       |  | 歌名 / 歌手 / 专辑                                |  |
|  | 当前播放状态         |  | 站长推荐语：这首歌适合在夜里慢慢读一篇旧文章。      |  |
|  +--------------------+  | 播放按钮 上一首 下一首 进度条 音量 播放模式          |  |
|                          | 歌词：当前句高亮，上一句/下一句淡出                 |  |
|                          +---------------------------------------------------+  |
+--------------------------------------------------------------------------------+

+--------------------------------------------------------------------------------+
|  歌单区                                                                          |
|  [今日电台] [夜读] [写代码] [旅行回忆]                         管理歌单(管理员) |
+--------------------------------------------------------------------------------+

+--------------------------------------------------------------------------------+
|  唱片架                                                                          |
|  +----------------+ +----------------+ +----------------+ +----------------+    |
|  | 封面            | | 封面            | | 封面            | | 封面            |    |
|  | 歌名 / 歌手     | | 歌名 / 歌手     | | 歌名 / 歌手     | | 歌名 / 歌手     |    |
|  | 标签 标签       | | 标签 标签       | | 标签 标签       | | 标签 标签       |    |
|  | 推荐语摘要      | | 推荐语摘要      | | 推荐语摘要      | | 推荐语摘要      |    |
|  | 编辑/下架(管理员)| | 编辑/下架(管理员)| | 编辑/下架(管理员)| | 编辑/下架(管理员)|   |
|  +----------------+ +----------------+ +----------------+ +----------------+    |
+--------------------------------------------------------------------------------+
```

### 6.2 Lyra 随身播放器

```text
              +------------------------------------------------+
              | Sakura Radio                              ×     |
              |------------------------------------------------|
              | +--------+  歌名                               |
              | | 封面   |  歌手 · 专辑                         |
              | | 旋转   |  “Lyra 正在播放这一首。”              |
              | +--------+                                      |
              |                                                |
              |    上一首     播放/暂停     下一首     模式      |
              |    ━━━━━━━━●━━━━━━━━━━━━  01:24 / 04:12        |
              |                                                |
              |    当前歌词高亮                                 |
              |    下一句歌词淡淡出现                           |
              |                                                |
              |    [播放列表]                     音量  ━━━●━━   |
              +------------------------------------------------+

  女仆工具栏：
  [聊天] [音乐] [换装] [截图] [关闭]
          ↑
        播放中时轻微律动，暂停时静止
```

### 6.3 移动端音乐页

```text
+--------------------------------------+
| 顶部移动导航                          |
+--------------------------------------+
| Hero: Sakura Radio                    |
| 标题、短副标题、柔雾背景               |
+--------------------------------------+
| 当前播放卡片                           |
| 封面                                  |
| 歌名 / 歌手                            |
| 播放按钮 进度条                        |
| 当前歌词 1-3 行                        |
+--------------------------------------+
| 横向滚动歌单                           |
| [今日电台] [夜读] [写代码]             |
+--------------------------------------+
| 唱片架单列或双列                       |
| +----------------------------------+  |
| | 封面 歌名 歌手 标签 推荐语         |  |
| +----------------------------------+  |
+--------------------------------------+
```

### 6.4 管理员编辑态

```text
音乐页顶部：
[新增歌曲] [管理歌单] [查看下架内容]

歌曲卡片管理员浮层：
[编辑] [上架/下架] [移入歌单] [排序拖拽]

歌曲编辑工作台：
┌──────────────────────────────────────┐
│ 编辑歌曲                              │
│ 歌名 / 歌手 / 专辑 / 年份 / 语言       │
│ 标签 / 风格                            │
│ 音频上传 / 封面上传                    │
│ 推荐语 / 心情短句                      │
│ 歌词类型：纯文本 | LRC                 │
│ 歌词输入区                             │
│ LRC 校验结果 / 播放预览                │
│                         取消   保存    │
└──────────────────────────────────────┘
```

## 7. 视觉设计样式说明

### 7.1 整体气质

音乐页应像 Chen404 首页自然延伸出来的一页：柔软、梦感、但控件清晰。它不是酷炫播放器，也不是后台表格。用户感受到的是“站长的私人电台”和“Lyra 递来一张唱片”。

关键词：

- 樱花粉
- 暖白纸感
- 柔雾玻璃
- 唱片架
- 夜读
- 私人推荐
- 轻盈陪伴

### 7.2 色彩

沿用现有色彩体系：

- 主色：`#fb7299`
- 主色浅层：`#ff85a7`
- 主色深层：`#e44d78`
- 页面背景：`#f4f5f7`
- 暖白表面：`rgba(255, 255, 255, 0.92)`
- 玻璃表面：`rgba(255, 251, 253, 0.72)`
- 边框：`rgba(255, 220, 232, 0.48)`
- 主文字：`#4f3c46`
- 次文字：`#8d7b84`
- 点缀冷色：淡紫雾 `#c7bdd9`

音乐页不要变成单一粉色页面。粉色用于激活态、播放态、重点按钮、歌词高亮和少量阴影。主体仍是暖白、纸感、柔灰和淡紫雾。

### 7.3 Hero

Hero 延续当前首页模式：

- 大幅背景图。
- 半透明粉白雾遮罩。
- 花瓣或柔光可以存在，但不可遮挡标题。
- 标题使用现有手写感 display 字体。
- 副标题使用清晰白色或浅暖白。
- Hero 高度建议桌面端 `68vh-76vh`，比首页略短，让用户更快进入播放器。
- Hero 底部可继续使用柔波浪过渡。

建议文案：

- 标题：`Sakura Radio`
- 副标题：`今晚，Lyra 为你调好一首歌。`
- 备用中文标题：`音乐馆`

默认背景建议：

- 樱花街景、唱片架、夜晚书桌、柔光房间。
- 避免强黑胶酒吧风、霓虹赛博风、强对比舞台风。

### 7.4 主播放器

主播放器是页面视觉核心。建议使用左右分栏：

- 左侧：封面唱片区。
- 右侧：歌曲信息、推荐语、控件、歌词。

桌面尺寸：

- 容器宽度：`min(1120px, calc(100vw - 48px))`
- 圆角：`28px-32px`
- 背景：暖白玻璃 + 微弱樱花渐变。
- 阴影：柔和玫瑰阴影，避免深灰重阴影。

封面：

- 使用正方形封面。
- 播放中可做唱片轻微旋转。
- 暂停时旋转停止。
- 封面缺失时使用默认唱片封面。

控件：

- 播放按钮为主要视觉按钮，圆形，樱花粉渐变。
- 上一首、下一首、模式、音量为轻按钮。
- 进度条使用樱花粉填充，轨道为浅粉灰。
- 所有按钮需要 hover、focus、disabled 状态。

### 7.5 歌词

LRC 歌词：

- 当前句高亮，颜色 `#e44d78` 或 `#fb7299`。
- 上下句使用 `#8d7b84`，透明度降低。
- 滚动动画控制在 `180ms-260ms`，不要过度跳动。
- 当前句可有极淡的粉色背景，不做粗重色块。

纯文本歌词：

- 使用纸张式文本容器。
- 保留换行。
- 最大高度内滚动。
- 行高建议 `1.8`。

空歌词：

- 显示安静空状态：`这首歌还没有写下歌词。`

### 7.6 歌单区

歌单应像一排轻量胶囊或小唱片标签：

- 当前歌单使用樱花粉浅底和粉色文字。
- 非当前歌单使用暖白玻璃底。
- 每个歌单可显示名称、歌曲数、短描述。
- 管理员可看到“管理歌单”入口。

不建议首版做复杂侧栏。当前站点首页是居中内容流，音乐页也应保持中央内容结构。

### 7.7 唱片架卡片

唱片卡片要比文章卡片更轻、更可浏览：

- 卡片圆角：`18px-22px`
- 封面比例：`1:1`
- 卡片宽度：桌面 `220px-260px`
- 网格：`repeat(auto-fill, minmax(220px, 1fr))`
- 推荐语最多两行。
- 标签使用浅粉胶囊。
- 当前播放卡片增加粉色描边和轻微上浮。

管理员操作不要常驻抢视觉。建议 hover 或卡片右上角显示小图标按钮：

- 编辑
- 上架/下架
- 移入歌单
- 拖拽排序

### 7.8 Lyra 小播放器

小播放器应与现有聊天面板同源，但更紧凑：

- 宽度：桌面 `320px-360px`
- 圆角：`22px-26px`
- 背景：白粉玻璃。
- 标题：`Sakura Radio`
- 不使用大面积黑色播放器风格。
- 如果 Lyra 靠左，小播放器出现在右侧；如果 Lyra 靠右，小播放器出现在左侧。
- 移动端小播放器固定在底部，宽度 `min(92vw, 360px)`。

小播放器与 `/music` 页面共享播放状态：

- 音乐页点歌后，小播放器打开显示同一首。
- 小播放器切歌后，音乐页当前播放状态同步。
- 关闭小播放器不停止播放。
- 暂停音乐后，音乐图标停止律动。

### 7.9 动效

只保留表达状态的动效：

- 播放中封面缓慢旋转。
- 音乐图标播放中轻微律动。
- 歌词切换平滑滚动。
- 卡片 hover 轻微上浮。
- 播放按钮点击有短反馈。

动效时间建议：

- hover：`180ms-240ms`
- 面板展开：`220ms-280ms`
- 歌词滚动：`180ms-260ms`
- 封面旋转：`18s-28s` 一圈，线性。

需支持 `prefers-reduced-motion`，减少或关闭旋转和律动。

## 8. 歌曲资料模型

### 8.1 MusicTrack

- `id`：歌曲 ID。
- `title`：歌名。
- `artist`：歌手。
- `album`：专辑，可为空。
- `releaseYear`：发行年份，可为空。
- `language`：语言，可为空。
- `genre`：风格，可为空。
- `tags`：标签列表。
- `audioFileId`：音频文件 ID。
- `audioUrl`：前台播放 URL。
- `coverFileId`：封面文件 ID，可为空。
- `coverUrl`：封面 URL，可为空。
- `lyricType`：`plain` 或 `lrc`。
- `lyrics`：歌词内容。
- `lyricSource`：歌词来源备注，可为空。
- `recommendation`：站长推荐语。
- `moodText`：心情短句，可为空。
- `status`：`draft`、`published`、`archived`。
- `sortOrder`：排序权重。
- `createdAt`：创建时间。
- `updatedAt`：更新时间。

### 8.2 MusicPlaylist

- `id`：歌单 ID。
- `name`：歌单名称。
- `description`：歌单描述。
- `coverFileId`：歌单封面文件 ID，可为空。
- `coverUrl`：歌单封面 URL，可为空。
- `openingText`：电台开场文案。
- `isDefault`：是否默认电台。
- `isPublic`：是否公开。
- `sortOrder`：排序权重。
- `createdAt`：创建时间。
- `updatedAt`：更新时间。

### 8.3 MusicPlaylistTrack

- `playlistId`：歌单 ID。
- `trackId`：歌曲 ID。
- `sortOrder`：歌单内排序。

## 9. 接口边界

### 9.1 公开接口

- `GET /music/tracks`：获取公开歌曲列表。
- `GET /music/tracks/{id}`：获取公开歌曲详情。
- `GET /music/playlists`：获取公开歌单列表。
- `GET /music/playlists/{id}`：获取公开歌单详情与歌曲。
- `GET /music/radio/default`：获取默认电台歌单，用于女仆播放器首次播放。

公开接口只返回 `published` 歌曲和 `isPublic = true` 歌单。

### 9.2 创作者接口

- `GET /music/tracks/mine`：当前用户可管理曲目。
- `GET /music/tracks/mine/{id}`：当前用户可管理曲目详情。
- `POST /music/tracks`：创建本人曲目。
- `PUT /music/tracks/{id}`：贡献者或管理员更新。
- `PATCH /music/tracks/{id}/status`：贡献者或管理员变更状态。
- `DELETE /music/tracks/{id}`：贡献者或管理员删除；服务端先清理歌单关系。

### 9.3 登录用户播放现场

- `GET /music/player/state`：读取 Redis 临时播放现场。
- `PUT /music/player/state`：覆盖保存并刷新 TTL。
- `DELETE /music/player/state`：清除播放现场。

Redis 不可用时播放本身继续工作，播放现场按尽力而为策略降级。

### 9.4 管理接口

管理接口需要管理员权限。

- `GET /admin/music/tracks`：管理员歌曲列表，包含草稿和归档。
- `GET /admin/music/tracks/{id}`：管理员歌曲详情。
- `POST /admin/music/tracks/ai/suggest`：AI 补全音乐信息。
- `POST /admin/music/tracks`：新增歌曲。
- `PUT /admin/music/tracks/{id}`：更新歌曲。
- `PATCH /admin/music/tracks/{id}/status`：上下架或归档歌曲。
- `DELETE /admin/music/tracks/{id}`：删除歌曲并先移除歌单关系。
- `GET /admin/music/playlists`：管理员歌单列表。
- `POST /admin/music/playlists`：新增歌单。
- `PUT /admin/music/playlists/{id}`：更新歌单。
- `PUT /admin/music/playlists/{id}/tracks`：保存歌单歌曲和排序。
- `DELETE /admin/music/playlists/{id}`：删除歌单。
- `PATCH /admin/music/playlists/{id}/default`：设置默认电台。

音频和封面上传复用现有文件上传能力：

- `POST /upload/music-audio`
- `POST /upload/music-cover`

保存歌曲时会将对应文件转为永久文件，并标记 `MUSIC_AUDIO` / `MUSIC_COVER`。

## 10. 歌词规则

- `plain`：前台静态展示，保留换行。
- `lrc`：前台解析时间戳并随播放高亮滚动。
- LRC 支持 `[mm:ss.xx]文本` 基础格式。
- 管理员保存 LRC 时进行格式校验。
- 格式错误不应导致整页崩溃，前台可降级为纯文本展示。
- 无歌词时显示安静空状态。

## 11. 播放状态

前端应建立统一音乐播放状态模块，供 `/music` 页面和 `Live2D` 小播放器共享。

状态包含：

- 当前队列。
- 当前歌曲。
- 当前播放时间。
- 播放/暂停状态。
- 音量。
- 播放模式。
- 当前歌单 ID。

当前持久化策略：

- 音量和播放模式保存在 `localStorage`，保证同一浏览器体验稳定。
- 游客的队列、当前歌曲、进度与模式保存在 `chen404.music.player-state.guest`。
- 登录用户通过 `/music/player/state` 保存队列、当前歌曲、进度与模式，服务端 Redis 默认 TTL 为 7 天。
- 登录、退出和身份切换时重新协调游客/用户现场；Redis 失败不阻断实际播放。

## 12. 错误与边界

- 没有公开歌曲：音乐页显示空状态，管理员看到新增入口，普通用户看到温和提示。
- 默认歌单为空：女仆音乐图标点击后弹出提示，不进入播放中状态。
- 音频加载失败：跳过当前歌曲并提示；如果队列都失败，停止播放。
- 歌曲下架：不再进入新队列；已在当前队列中的歌曲可在下一次刷新后消失。
- 封面缺失：使用默认唱片封面。
- 移动端：小播放器固定在可视区域内，避免遮挡 Lyra 或核心阅读内容。

## 13. 测试范围

### 13.1 后端

- 公开接口只返回公开内容。
- 创作者所有权、管理员全站管理与运行时开关校验。
- 歌曲新增、编辑、上下架、删除。
- 歌单新增、编辑、默认歌单设置、排序保存。
- LRC 格式校验。
- 删除歌曲时歌单关系清理、歌单删除和音乐文件引用同步。
- 登录用户播放现场的保存、读取、删除与 Redis 降级。

### 13.2 前端

- `/music` 页面公开浏览。
- 创作者本人编辑入口、管理员治理入口和普通访客隐藏规则。
- 歌曲播放、暂停、切歌、进度、音量、播放模式。
- LRC 歌词高亮滚动与纯文本歌词展示。
- 女仆音乐图标点击后弹出并自动播放。
- 小播放器展开/收起不重置播放。
- 移动端布局。
- `prefers-reduced-motion` 下关闭不必要动画。
- 当前 `Music.layout.test.mjs` 有 2 个布局契约与实现不一致的失败项，详见仓库扫描报告；修复前不能把现有前端音乐测试表述为全绿。

## 14. 首版决策

- 音乐页公开给所有访客访问，知友创作者可维护本人曲目，管理员可维护全站曲目与歌单。
- 首版支持多个公开歌单，并要求存在一个默认电台歌单。
- 上传音频和封面沿用现有文件上传、文件引用和存储配置。
- 音乐模块只保存文件 ID 和业务元数据。
- 音乐页接入现有功能 Hero 配置体系，新增 `music` 配置 key，未配置时使用默认 Sakura Radio 背景。
- 导航新增“音乐馆”，Lyra 工具栏新增音乐按钮。

## 15. 已完成实施路径

1. 后端音乐数据表、实体、DTO、公开/创作者/管理接口已完成。
2. 前端音乐 API、统一播放器 store 和 Live2D 随身播放器已完成。
3. `/music` 公开浏览、播放和歌词展示已完成。
4. 独立歌曲工作台、歌单管理、默认电台、排序与删除已完成。
5. 文件永久化、统一引用和登录用户跨会话播放现场已完成。
6. 自动化测试接入仍需补齐，见第 13 节和扫描报告。

## 16. 后续优化清单

1. 增加歌单封面上传或从歌单第一首歌曲封面自动继承。
2. 支持 `/music?playlist={id}` 和 `/music?track={id}` 稳定深链，便于分享指定歌单或歌曲。
3. 增加 Media Session API，支持系统媒体键、锁屏封面和浏览器媒体中心。
4. 增加播放统计、最近播放、收藏或喜欢，先只做站长侧统计也可。
5. 补播放器异常策略：当队列全部失败时停止播放并给出明确提示。
6. 修复现有布局契约测试，并将 Node 测试与 Playwright 冒烟测试纳入统一前端命令。
